#include<stdio.h>
// #include<conio.h>

int main(){
    unsigned var = 8, a;
    // Left shift
    a = var << 1;
    printf("Left shift = %d\n", a);

    // Left shift
    a = var >> 1;
    printf("Right shift = %d\n", a);
    
    return 0;
}
